{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editer l'offre de voyage</h1>
     <a href="{{ path('offrevoyage_index') }}">listes des offres de voyage</a>
     <hr />
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

{% endblock %}
